Student Feedback Form – React + Vite

This project is built using React + Vite

This link opens the optimized production build on the local machine:
http://localhost:3000/

Network Deployment (Accessible to others in same network)
Others connected to the same Wi-Fi / LAN can access the deployed build using:
http://10.142.27.19:3000/


To run the project locally:

```bash
npm install
npm run dev
